# Database file path (SQLite). Default: visitors.db
DB_PATH=visitors.db

# Port to listen on. Default: 4009
PORT=4009

# Salt used to hash visitor IPs for privacy (any random string).
# Change this to a unique secret for your installation.
IP_SALT=change-me-to-a-random-string